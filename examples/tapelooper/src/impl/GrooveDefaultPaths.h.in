#pragma once

// Generated by CMake (configure_file); see examples/groover/CMakeLists.txt.
// A '#' in either path (e.g. a MidiDrums filename like "str_4#4_1_c_v1.mid")
// cannot survive as a compiler -D command-line definition, so these paths are
// baked into a real header instead.
inline constexpr const char* kAbacDspDrumSamplesDir = "@ABACDSP_DRUM_SAMPLES_DIR@";
inline constexpr const char* kAbacDspMidiDrumsDir = "@ABACDSP_MIDI_DRUMS_DIR@";
// Relative to kAbacDspMidiDrumsDir, not absolute - this is also the form
// GrooveKit::requestLoad()/currentGrooveName() use for every other groove.
inline constexpr const char* kAbacDspDefaultGrooveName = "@ABACDSP_DEFAULT_GROOVE_NAME@";
