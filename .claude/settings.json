{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.cpp|*.h|*.hpp) clang-format -i \"$f\" ;; */CMakeLists.txt|*.cmake) gersemi --in-place \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting..."
          }
        ]
      }
    ]
  }
}
