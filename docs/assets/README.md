# Documentation assets

Screenshots used in the root `README.md` and `docs/*.md`. These are snapshots of a specific
UI state, not generated artifacts - re-capture and replace them whenever the relevant
example's UI changes (e.g. after regenerating it with `JuceStandaloneGenerator`, or any
hand-edit to its layout/theme).
